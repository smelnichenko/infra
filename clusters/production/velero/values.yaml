initContainers:
  - name: velero-plugin-for-aws
    image: velero/velero-plugin-for-aws:v1.11.1
    volumeMounts:
      - name: plugins
        mountPath: /target
credentials:
  useSecret: true
  existingSecret: velero-credentials
configuration:
  backupStorageLocation:
    - name: default
      provider: aws
      bucket: velero
      config:
        region: us-east-1
        s3ForcePathStyle: "true"
        s3Url: http://192.168.11.5:9000
  volumeSnapshotLocation: []
  defaultVolumesToFsBackup: true
  features: ""
  # Keep only the most recent maintenance Job per repository (chart default is 3).
  # With 12 BackupRepositories that default leaves 36 completed maintenance pods
  # lingering; one is plenty to inspect the last run on this single node.
  repositoryMaintenanceJob:
    repositoryConfigData:
      global:
        keepLatestMaintenanceJobs: 1
deployNodeAgent: true
snapshotsEnabled: false
schedules:
  schnappy-daily:
    disabled: false
    schedule: "0 2 * * *"
    useOwnerReferencesInBackup: false
    template:
      ttl: 168h
      includedNamespaces:
        - schnappy-production
      defaultVolumesToFsBackup: true
  full-weekly:
    disabled: false
    schedule: "0 3 * * 0"
    useOwnerReferencesInBackup: false
    template:
      ttl: 720h
      includedNamespaces:
        - "*"
      defaultVolumesToFsBackup: true
upgradeCRDs: false
kubectl:
  image:
    repository: docker.io/bitnami/kubectl
    tag: latest
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 1000m
    memory: 512Mi
