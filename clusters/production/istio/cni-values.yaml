global:
  platform: k3s

cni:
  ambient:
    enabled: true
  cniConfDir: /mnt/storage/k3s/agent/etc/cni/net.d
  cniBinDir: /opt/cni/bin
  resources:
    requests:
      cpu: 25m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi
