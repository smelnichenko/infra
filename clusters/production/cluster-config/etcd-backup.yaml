# Hourly etcd snapshot → the Pi backup store (off-node DR backup).
#
# Gap this closes: etcd on `ten` had only the in-tree static-pod auto-snapshots on
# the same NVMe as live etcd — a single point of failure, and the only off-node copy
# of cluster state. (Plan 071-A proposed this and was never built.)
#
# Design: a kube-system CronJob (GitOps, reuses ESO/Vault — no host binaries or
# cred files on `ten`). Two etcd-image initContainers take + verify the snapshot
# (the etcd image has no shell, so each is a single etcdctl invocation), then an rclone
# container uploads it to the Pi backup store's `etcd-backups` bucket with a timestamped name
# and prunes >7d. (rclone, not MinIO's mc: MinIO withdrew the mc images on 2026-09-24 —
# quay.io/minio/mc 401, docker.io/minio/mc gone — and the store is versitygw anyway.)
# hostNetwork is used to reach etcd on 127.0.0.1:2379; the snapshot
# container runs as root only to read the root-owned etcd server key (read-only).
#
# Restore (manual, host-level): copy the .db to `ten`, `etcdctl snapshot restore`,
# repoint the etcd static pod's data-dir, restart kubelet. See ops DR-PROCEDURE.md.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: etcd-backup
  namespace: kube-system
# No RBAC: the job never touches the Kubernetes API — it snapshots etcd directly
# and uploads to the backup store.
automountServiceAccountToken: false
---
# Pi backup-store credentials (same store Velero/CNPG back up to — secret/schnappy/velero).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: etcd-backup-store
  namespace: kube-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: etcd-backup-store
  data:
    - secretKey: ACCESS_KEY
      remoteRef:
        key: secret/data/schnappy/velero
        property: access_key
    - secretKey: SECRET_KEY
      remoteRef:
        key: secret/data/schnappy/velero
        property: secret_key
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: etcd-backup
  namespace: kube-system
  labels:
    app: etcd-backup
spec:
  schedule: "0 * * * *"            # hourly, on the hour
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 600
      template:
        metadata:
          labels:
            app: etcd-backup
        spec:
          restartPolicy: Never
          hostNetwork: true         # reach etcd on 127.0.0.1:2379
          serviceAccountName: etcd-backup
          automountServiceAccountToken: false
          # Single node today; tolerate the control-plane taint so this still lands
          # on the etcd node if the cluster ever grows / gets tainted.
          tolerations:
            - key: node-role.kubernetes.io/control-plane
              operator: Exists
              effect: NoSchedule
          securityContext:
            seccompProfile:
              type: RuntimeDefault
          initContainers:
            - name: snapshot
              image: registry.k8s.io/etcd:3.6.5-0
              command: ["etcdctl"]
              args:
                - "--endpoints=https://127.0.0.1:2379"
                - "--cacert=/etc/kubernetes/pki/etcd/ca.crt"
                - "--cert=/etc/kubernetes/pki/etcd/server.crt"
                - "--key=/etc/kubernetes/pki/etcd/server.key"
                - "snapshot"
                - "save"
                - "/backup/etcd.db"
              env:
                - { name: ETCDCTL_API, value: "3" }
              securityContext:
                runAsUser: 0          # read the root-owned etcd server key (RO mount)
                readOnlyRootFilesystem: true
                allowPrivilegeEscalation: false
                capabilities: { drop: ["ALL"] }
              resources:
                requests: { cpu: 50m, memory: 64Mi, ephemeral-storage: 256Mi }
                limits: { memory: 256Mi, ephemeral-storage: 2Gi }
              volumeMounts:
                - { name: etcd-certs, mountPath: /etc/kubernetes/pki/etcd, readOnly: true }
                - { name: backup, mountPath: /backup }
            - name: prepare
              # etcdctl writes the snapshot 0600 root; make it readable so the
              # non-root verify/upload containers can access it. Reuses the rclone image
              # (pulled for the upload anyway) to avoid an extra image just for chmod.
              image: rclone/rclone:1.75.1@sha256:45401ad7410db1d67ffdb58e19059ad20b0d8e0285a60e38bbec55cc1019c7a5
              command: ["/bin/sh", "-ec"]
              args: ["chmod 0644 /backup/etcd.db; ls -l /backup/etcd.db"]
              securityContext:
                runAsUser: 0
                readOnlyRootFilesystem: true
                allowPrivilegeEscalation: false
                capabilities: { drop: ["ALL"] }
              resources:
                requests: { cpu: 20m, memory: 32Mi, ephemeral-storage: 64Mi }
                limits: { memory: 64Mi, ephemeral-storage: 128Mi }
              volumeMounts:
                - { name: backup, mountPath: /backup }
            - name: verify
              image: registry.k8s.io/etcd:3.6.5-0
              # snapshot status/restore moved from etcdctl to etcdutl in etcd 3.5+.
              command: ["etcdutl"]
              args: ["snapshot", "status", "/backup/etcd.db", "-w", "table"]
              securityContext:
                runAsNonRoot: true
                runAsUser: 65534
                readOnlyRootFilesystem: true
                allowPrivilegeEscalation: false
                capabilities: { drop: ["ALL"] }
              resources:
                requests: { cpu: 50m, memory: 64Mi, ephemeral-storage: 256Mi }
                limits: { memory: 256Mi, ephemeral-storage: 2Gi }
              volumeMounts:
                - { name: backup, mountPath: /backup, readOnly: true }
          containers:
            - name: upload
              image: rclone/rclone:1.75.1@sha256:45401ad7410db1d67ffdb58e19059ad20b0d8e0285a60e38bbec55cc1019c7a5
              command: ["/bin/sh", "-ec"]
              args:
                - |
                  TS=$(date +%Y%m%d-%H%M%S)
                  rclone mkdir pi:etcd-backups
                  rclone copyto /backup/etcd.db "pi:etcd-backups/etcd-${TS}.db"
                  # age is the object's modtime: a fresh snapshot is now; an mc-era upload, its upload time
                  rclone delete --min-age 7d pi:etcd-backups || true
                  echo "etcd-backup OK: etcd-${TS}.db uploaded; current backups:"
                  rclone lsl pi:etcd-backups | sort -k2,3 | tail -5
              env:
                # the remote "pi" is configured by environment alone: no config file on the read-only root
                - { name: RCLONE_CONFIG, value: "" }
                - { name: RCLONE_CONFIG_PI_TYPE, value: s3 }
                - { name: RCLONE_CONFIG_PI_PROVIDER, value: Other }
                - { name: RCLONE_CONFIG_PI_ENDPOINT, value: "http://192.168.11.5:9000" }
                - { name: RCLONE_CONFIG_PI_ACCESS_KEY_ID, valueFrom: { secretKeyRef: { name: etcd-backup-store, key: ACCESS_KEY } } }
                - { name: RCLONE_CONFIG_PI_SECRET_ACCESS_KEY, valueFrom: { secretKeyRef: { name: etcd-backup-store, key: SECRET_KEY } } }
                - { name: HOME, value: /tmp }
              securityContext:
                runAsNonRoot: true
                runAsUser: 65534
                readOnlyRootFilesystem: true
                allowPrivilegeEscalation: false
                capabilities: { drop: ["ALL"] }
              resources:
                requests: { cpu: 50m, memory: 64Mi, ephemeral-storage: 256Mi }
                limits: { memory: 128Mi, ephemeral-storage: 2Gi }
              volumeMounts:
                - { name: backup, mountPath: /backup, readOnly: true }
                - { name: tmp, mountPath: /tmp }
          volumes:
            - name: etcd-certs
              hostPath:
                path: /etc/kubernetes/pki/etcd
                type: Directory
            - name: backup
              emptyDir:
                sizeLimit: 2Gi
            - name: tmp
              emptyDir:
                sizeLimit: 64Mi
