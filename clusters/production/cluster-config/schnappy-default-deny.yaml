apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: schnappy-default-deny
  namespace: schnappy
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Intra-namespace traffic (sidecars communicate on app ports)
    - from:
        - podSelector: {}
    # Istio gateway: allow external traffic (HAProxy) on 80/443
    - ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 80
    # Cross-namespace ingress (forgejo, argocd, woodpecker → schnappy services)
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: forgejo
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: argocd
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: woodpecker
      ports:
        - protocol: TCP
          port: 8080
        - protocol: TCP
          port: 443
    # Kubelet probes (node → pod)
    - ports:
        - protocol: TCP
          port: 15021
  egress:
    # DNS
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # Intra-namespace traffic (sidecars communicate on app ports)
    - to:
        - podSelector: {}
    # Istio control plane (istiod cert signing, xDS)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: istio-system
      ports:
        - protocol: TCP
          port: 15012
        - protocol: TCP
          port: 15010
        - protocol: TCP
          port: 15014
        - protocol: TCP
          port: 443
    # Cross-namespace egress (Istio gateway → non-mesh services)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: forgejo
      ports:
        - protocol: TCP
          port: 3000
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: argocd
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 8080
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: woodpecker
      ports:
        - protocol: TCP
          port: 8000
        - protocol: TCP
          port: 9000
