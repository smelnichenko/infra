gitea:
  admin:
    existingSecret: forgejo-admin
  config:
    server:
      DOMAIN: git.pmon.dev
      ROOT_URL: "https://git.pmon.dev/"
      SSH_DOMAIN: git.pmon.dev
      DISABLE_SSH: true
      LFS_START_SERVER: true
    database:
      DB_TYPE: sqlite3
    service:
      DISABLE_REGISTRATION: false
      ALLOW_ONLY_EXTERNAL_REGISTRATION: true
    actions:
      ENABLED: false
    webhook:
      ALLOWED_HOST_LIST: "*"
    oauth2:
      # Woodpecker builds a pipeline's clone credential from the owner's OAuth access token when
      # the webhook arrives and only then queues the pipeline; with the 1 h default a queued
      # pipeline could outlive its token and die in the clone step ("could not read Username").
      ACCESS_TOKEN_EXPIRATION_TIME: 86400
    repository:
      DEFAULT_BRANCH: main
      DEFAULT_PRIVATE: true
    log:
      LEVEL: Warn
image:
  rootless: true
ingress:
  enabled: false
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 1Gi
persistence:
  enabled: true
  size: 20Gi
  storageClass: local-path
redis-cluster:
  enabled: false
postgresql:
  enabled: false
postgresql-ha:
  enabled: false
